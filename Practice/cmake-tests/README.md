# CMake with GTest example

---

This is a simple example of using CMake with GoogleTests so I don't have to
re-lookup how to set everything up.

To build:

``` 
cd build
cmake ..
make
```

To run the tests:

```
./runUnitTests
```